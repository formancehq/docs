---
title: Modulr
description: Learn how to use the Modulr Connector to connect your Formance
  Platform with your Modulr account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Modulr from '@site/docs/payments/partials/connectors/_modulr.mdx';
import ModulrPostInstall from '@site/docs/payments/partials/connectors/_modulr_post.mdx';
import ModulrConfig from '@site/docs/payments/partials/connectors/_modulr_config.json';
import ReactJson from 'react-json-view';

## Prerequisites

Before you begin, you need to have a Modulr account. Make sure you have a Modulr API key and secret, with the least amount of permissions required to access the functionality of the Modulr Connector you plan to use.

<ApiWarning/>

<Modulr/>

## Installation

<StackCmd>

```shell
fctl payments connectors install modulr config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/modulr" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

<ReactJson
  src={ModulrConfig}
  theme="ocean"
  displayDataTypes={false}
  collapsed={1}
  name={false}
  indentWidth={2}
/>

<ModulrPostInstall/>
