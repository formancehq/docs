---
title: Wise
description: Learn how to use the Wise Connector to connect your Formance
  Platform with your Wise account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Wise from '@site/docs/payments/partials/connectors/_wise.mdx';
import WisePostSetup from '@site/docs/payments/partials/connectors/_wise_post.mdx';

## Prerequisites

Before you begin, you need to have a Wise account. Make sure you have a Wise API key and secret, with the least amount of permissions required to access the functionality of the Wise Connector you plan to use.

<ApiWarning/>

## Setup

<Wise/>

<StackCmd>

```shell
fctl payments connectors install wise config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/wise" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

```json
{"apiKey":"string","name":"string","pageSize":"25","pollingPeriod":"2m","webhookPublicKey":"string"}
```

<WisePostSetup/>
