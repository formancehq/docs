---
title: Atlar
description: Learn how to use the Atlar Connector to connect your Formance
  Platform with your Atlar account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Atlar from '@site/docs/payments/partials/connectors/_atlar.mdx';
import AtlarPostInstall from '@site/docs/payments/partials/connectors/_atlar_post.mdx';
import JSONSchemaViewer from '@theme/JSONSchemaViewer';

## Prerequisites

Before you begin, you need to have a Atlar account. Make sure you have a Atlar API key and secret, with the least amount of permissions required to access the functionality of the Atlar Connector you plan to use.

<ApiWarning/>

## Setup

<Atlar/>

## Install

<StackCmd>

```shell
fctl payments connectors install atlar config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/atlar" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

```json
{"accessKey":"string","baseUrl":"string","name":"string","pageSize":"25","pollingPeriod":"2m","secret":"string"}
```

Schema viewer:

<CodeBlock language="json-schema">{JSON.stringify(rawData, null, 2)}</CodeBlock>

## Post-Install

<AtlarPostInstall/>
