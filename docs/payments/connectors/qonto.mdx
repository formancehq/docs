---
title: Qonto
description: Learn how to use the Qonto Connector to connect your Formance
  Platform with your Qonto account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Qonto from '@site/docs/payments/partials/connectors/_qonto.mdx';
import QontoPostInstall from '@site/docs/payments/partials/connectors/_qonto_post.mdx';
import QontoConfig from '@site/docs/payments/partials/connectors/_qonto_config.json';
import ReactJson from 'react-json-view';

## Prerequisites

Before you begin, you need to have a Qonto account. Make sure you have a Qonto API key and secret, with the least amount of permissions required to access the functionality of the Qonto Connector you plan to use.

<ApiWarning/>

<Qonto/>

## Installation

<StackCmd>

```shell
fctl payments connectors install qonto config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/qonto" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

<ReactJson
  src={QontoConfig}
  theme="ocean"
  displayDataTypes={false}
  collapsed={1}
  name={false}
  indentWidth={2}
/>

<QontoPostInstall/>
