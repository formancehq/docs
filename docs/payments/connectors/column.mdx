---
title: Column
description: Learn how to use the Column Connector to connect your Formance
  Platform with your Column account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Column from '@site/docs/payments/partials/connectors/_column.mdx';
import ColumnPostInstall from '@site/docs/payments/partials/connectors/_column_post.mdx';
import JSONSchemaViewer from '@theme/JSONSchemaViewer';

## Prerequisites

Before you begin, you need to have a Column account. Make sure you have a Column API key and secret, with the least amount of permissions required to access the functionality of the Column Connector you plan to use.

<ApiWarning/>

## Setup

<Column/>

## Install

<StackCmd>

```shell
fctl payments connectors install column config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/column" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

```json
{"apiKey":"string","endpoint":"string","name":"string","pageSize":"25","pollingPeriod":"2m"}
```

Schema viewer:

<CodeBlock language="json-schema">{JSON.stringify(rawData, null, 2)}</CodeBlock>

## Post-Install

<ColumnPostInstall/>
