---
title: Bankingcircle
description: Learn how to use the Bankingcircle Connector to connect your
  Formance Platform with your Bankingcircle account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Bankingcircle from '@site/docs/payments/partials/connectors/_bankingcircle.mdx';
import BankingcirclePostInstall from '@site/docs/payments/partials/connectors/_bankingcircle_post.mdx';
import JSONSchemaViewer from '@theme/JSONSchemaViewer';

## Prerequisites

Before you begin, you need to have a Bankingcircle account. Make sure you have a Bankingcircle API key and secret, with the least amount of permissions required to access the functionality of the Bankingcircle Connector you plan to use.

<ApiWarning/>

## Setup

<Bankingcircle/>

## Install

<StackCmd>

```shell
fctl payments connectors install bankingcircle config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/bankingcircle" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

```json
{"authorizationEndpoint":"string","endpoint":"string","name":"string","pageSize":"25","password":"string","pollingPeriod":"2m","userCertificate":"string","userCertificateKey":"string","username":"string"}
```

Schema viewer:

<CodeBlock language="json-schema">{JSON.stringify(rawData, null, 2)}</CodeBlock>

## Post-Install

<BankingcirclePostInstall/>
