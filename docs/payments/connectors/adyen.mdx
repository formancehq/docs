---
title: Adyen
description: Learn how to use the Adyen Connector to connect your Formance
  Platform with your Adyen account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Adyen from '@site/docs/payments/partials/connectors/_adyen.mdx';
import AdyenPostInstall from '@site/docs/payments/partials/connectors/_adyen_post.mdx';
import AdyenConfig from '@site/docs/payments/partials/connectors/_adyen_config.json';
import ReactJson from 'react-json-view'

## Prerequisites

Before you begin, you need to have a Adyen account. Make sure you have a Adyen API key and secret, with the least amount of permissions required to access the functionality of the Adyen Connector you plan to use.

<ApiWarning/>

<Adyen/>

## Installation

<StackCmd>

```shell
fctl payments connectors install adyen config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/adyen" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

<ReactJson
  src={AdyenConfig}
  theme="ocean"
  displayDataTypes={false}
  collapsed={1}
  name={false}
  indentWidth={2}
/>

<AdyenPostInstall/>
