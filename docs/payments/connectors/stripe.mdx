---
title: Stripe
description: Learn how to use the Stripe Connector to connect your Formance
  Platform with your Stripe account.
---

import {StackCmd} from '@site/src/components/StackCmd';
import JsonViewer from '@site/src/components/JsonViewer';
import ApiWarning from '@site/docs/payments/partials/_api_key.mdx';
import Stripe from '@site/docs/payments/partials/connectors/_stripe.mdx';
import StripePostInstall from '@site/docs/payments/partials/connectors/_stripe_post.mdx';
import StripeConfig from '@site/docs/payments/partials/connectors/_stripe_config.json';

## Prerequisites

Before you begin, you need to have a Stripe account. Make sure you have a Stripe API key and secret, with the least amount of permissions required to access the functionality of the Stripe Connector you plan to use.

<ApiWarning/>

<Stripe/>

## Installation

<StackCmd>

```shell
fctl payments connectors install stripe config.json
```

```shell
curl -X POST "https://$STACK.formance.cloud/api/payments/connectors/stripe" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $TOKEN" \
-d @config.json
```

</StackCmd>

With config.json containing:

<JsonViewer data={StripeConfig} theme='ocean' />

<StripePostInstall/>
